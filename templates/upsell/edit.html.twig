{% extends 'base_backofficeclient.html.twig' %}

{% block title %}Editer un upsell{% endblock %}

{% block body %}
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h2>Editer un upsell</h2>
            </div>
            <div>
                <a href="{{ path("app_upsell") }}" class="btn btn-primary">Gérer les upsells</a><br /><br />
            </div>
        </div>

        <ul class="nav nav-tabs mb-3">
            <li class="nav-item">
                <a class="nav-link text-white" aria-current="page" href="{{ path("app_service") }}">Services</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="#">Upsells</a>
            </li>
        </ul>

        {{ form(form) }}
    </div>
{% endblock %}
